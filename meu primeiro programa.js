# js.programacaocriativa.sofia
function setup() {
  createCanvas(600, 600);
  background("violet")
}


function draw() {
  
   stroke("baby blue");
  fill("cyan");

//console.log (mouseIsPressed);

if (mouseIsPressed); {
  rect(mouseX, mouseY, 20, 35);
}
}
